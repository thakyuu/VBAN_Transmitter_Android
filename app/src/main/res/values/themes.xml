<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="Theme.VbanTransmitter" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Window background and status bar styles. -->
        <item name="android:windowBackground">@color/background_dark</item>
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:windowLightStatusBar">false</item>
        
        <!-- Primary branding. -->
        <item name="colorPrimary">@color/primary_neon</item>
        <item name="colorSecondary">@color/secondary_neon</item>
        <item name="colorContainer">@color/card_dark</item>
    </style>
</resources>
